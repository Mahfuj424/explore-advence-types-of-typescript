"use strict";
{
    // maped type
    const arrOfNumber = [2, 3, 4];
    const convertStringArr = arrOfNumber.map(number => number.toString());
    console.log(convertStringArr);
    const area1 = {
        height: '100',
        width: 50
    };
}
